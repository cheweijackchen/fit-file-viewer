# How to setup project
This is a simple boiler plate for project running on docker.

1. Run docker container with command: `./oconf devcontainer start`
2. Get in docker container bash with command: `./oconf devcontainer bash`
3. Create quasar project following the steps in the official document
4. Configure ...
    